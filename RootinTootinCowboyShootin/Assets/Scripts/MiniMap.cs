using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MiniMap : MonoBehaviour
{
    public Image map;
    public Camera mapCamera; //unsure if this will be needed but it may be for the rough version of the mechanic
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void ChangeMap() //I have no idea what I wanna use this for right now tbh
    {
    
    }
}
