using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//This is attached to the reload time text mesh for the sole purpose of finding it easier
//No code is needed here, and this reload UI solution is temporary so ignore this file
public class ReloadTextMesh : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
